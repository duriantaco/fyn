[project]
name = "setuptools_editable"
version = "0.1.0"
description = "Default template for a setuptools project"
authors = [
    {name = "fyn contributors"},
]
dependencies = ["iniconfig"]
requires-python = ">=3.11,<3.13"
license = {text = "MIT"}

[project.optional-dependencies]
anyio = ["anyio>=3.3.0"]
