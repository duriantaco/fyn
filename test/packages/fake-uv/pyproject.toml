[project]
name = "fyn"
version = "0.1.0"
requires-python = ">=3.8"

[tool.fyn.build-backend.data]
scripts = "scripts"

[build-system]
requires = ["fyn_build>=0.8.0,<0.11"]
build-backend = "fyn_build"
